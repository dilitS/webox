---
captured: inferred
account: webox-test (synthetic)
command: devil vhost list
sanitized: domain prefixes -> webox-test.smallhost.pl, IP ->
  203.0.113.10 (RFC 5737 documentation block)
notes: Used by SetupSSL to discover the account IP that the panel
  associates with subdomains. Parser only needs the IP — the remaining
  columns are surfaced for diagnostics.
---
