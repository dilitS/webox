// Package env will host the .env parsing, diff, and interactive merge
// engine used by the /env tab.
//
// The whole /env feature is STRETCH (v0.2+) per docs/ROADMAP.md §3.3,
// so this package ships only as a placeholder in MVP. See
// docs/DESIGN.md §11.1 for the planned architecture of the bidirectional
// Env Merger.
package env
