// Package {{.PackageName}} is the HostingProvider adapter for {{.DisplayName}}.
//
// Generated by `webox provider new {{.Name}}` from
// assets/provider-template/. **Every method currently returns
// [ErrNotImplemented] — replace the bodies one by one following the
// walkthrough at docs/contributing/PROVIDER.md.**
//
// Preset: {{.Preset}}. Replace the transport scaffolding with the
// actual UAPI / SSH / vendor-API calls your panel exposes. Keep
// command tokens as package-private constants and validate every
// user-supplied identifier (subdomain, db name, node version) before
// substituting it into a command string.
//
// All parsers MUST follow the same defensive contract as the canonical
// `providers/smallhost` adapter:
//
//   - strip ANSI escapes,
//   - cap output size at 1 MiB (providers.ErrOutputTooLarge),
//   - use named regex groups,
//   - return providers.ErrUnknownOutputFormat for unrecognised shapes,
//   - never include raw secret bytes in error messages.
//
// Golden fixtures live in testing/fixtures/{{.Name}}/ (see the README.md
// in that directory for sourcing + sanitisation policy).
package {{.PackageName}}
